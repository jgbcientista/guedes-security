spring:
  datasource:
    url: jdbc:mysql://localhost:3306/joaoguedesdb?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    database: MYSQL
    database-platform: org.hibernate.dialect.MySQL5Dialect
server:
 port: 8090
     